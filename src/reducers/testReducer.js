const INITIAL_STATE = 'hello world';

export default (state = INITIAL_STATE, action) => {
    // switch (action.type) {
    //     case TEST:
    //         return action.payload;
    //     default:
    //         return state;
    // }
    return 'hello world'
};

